/* global mocksClear */
// import { encodeToNumber } from './makeSubs';

// describe('encodeToNumber', () => {
//   it('should encode string to number', () => {
//     const encoded = encodeToNumber('1m_BTC_USDT');

//     console.log(encoded);

//     // expect(spySendFn).toHaveBeenCalledTimes(10);

//     // expect(spySendFn).toHaveBeenLastCalledWith(JSON.stringify(pingMsg.msg));

//     // expect(spyCbFn).toHaveBeenCalledTimes(10);
//   });
// });
